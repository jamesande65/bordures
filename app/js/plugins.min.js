'use strict';

var btn = document.querySelector(".js-btn");
var mobileMenu = document.querySelector(".js-mob-menu");


function togglePopup() {
    mobileMenu.classList.toggle("active");
}

if (btn && mobileMenu) {
    btn.addEventListener("click", togglePopup);
}